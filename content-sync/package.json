{
	"name": "content-sync",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"wrangler:types": "wrangler types --experimental-include-runtime"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.5.2",
		"typescript": "^5.5.2",
		"vitest": "2.1.8",
		"wrangler": "^3.60.3"
	},
	"dependencies": {
		"octokit": "^4.0.3"
	}
}
