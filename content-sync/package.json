{
  "name": "content-sync",
  "version": "1.0.0",
  "private": true,
  "author": "Kynson Szetau (https://kynsonszetau.com/)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kynson/content-sync.git"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "doppler run --mount .dev.vars --format env -- wrangler dev",
    "test": "doppler run --mount .dev.vars --format env -- vitest",
    "wrangler:types": "doppler run --mount .dev.vars --format env -- wrangler types",
    "wrangler": "wrangler",
    "smee": "smee",
    "lint": "eslint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "@faker-js/faker": "^9.9.0",
    "eslint": "^10.9.0",
    "smee-client": "^2.0.4",
    "typescript": "^5.9.3",
    "vitest": "4.1.11",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.3.0",
    "@octokit/request": "^10.0.15",
    "@octokit/webhooks": "^14.2.0",
    "cf-workers-utils": "^1.1.0",
    "common": "workspace:^"
  },
  "type": "module"
}