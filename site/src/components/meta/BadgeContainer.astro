---

---

{/* BadgeContainer styles */}
<style>
  .badge-container :global(sup) {
    vertical-align: super;
    margin-left: var(--spacing);
  }

  .badge-container :global(p) {
    display: inline-block;
  }
</style>

{/* BageContainer content */}
<div class="badge-container flex flex-wrap gap-4 not-prose">
  <slot />
</div>
