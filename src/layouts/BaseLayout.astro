---
interface Props {
	extraClasses: string;
}

import Head from '../components/Head.astro';

const { extraClasses } = Astro.props;
---

{/* BaseLayout content */}
<!DOCTYPE html>
<html lang="en" class="m-auto bg-gray-950 text-blue-50 min-h-dvh max-w-screen-2xl">
	<Head />
	<body class="min-h-dvh p-6" class:list={extraClasses}>
		<slot />
	</body>
</html>
